export enum project_state {
    PROPOSE = 'Propose',
    OPEN = 'Open',
    CLOSE = 'Close',
}

export enum access_permit {
    READ = 'Read',
    CREATE = 'Create',
    UPDATE = 'Update',
    DELETE = 'Delete',
}